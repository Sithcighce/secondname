# Fun English Learning App (Demo)

一个仿抖音体验的英语学习应用 Demo，结合了短视频流与沉浸式互动课程（类似多邻国）。用户在观看趣味视频后，可以无缝进入关联的英语挑战，通过时间轴式的交互体验学习单词和知识点。

## ✨ 核心功能

### 1. 沉浸式视频流 (Feed)
*   **仿抖音交互**：全屏竖向滑动切换视频 (CSS Scroll Snap)。
*   **智能播放**：
    *   滑入自动播放，滑出自动暂停。
    *   **优先有声播放**：尝试开启声音播放，若浏览器策略拦截，则自动降级为静音播放并提示用户开启声音。
*   **无限刷**：模拟无限数据流，支持循环滑动。
*   **多屏适配**：针对不同视频素材（横屏/竖屏）自动调整显示布局，支持留黑边或全屏填充。

### 2. 交互式课程 (Interactive Lesson)
*   **无缝衔接**：点击视频右上角的 "Daily Quiz" 按钮，通过过渡动画平滑进入课程。
*   **时间轴模式 (Timeline UI)**：
    *   摒弃传统翻页，采用**垂直流式生长**布局。
    *   新内容出现时自动平滑滚动对齐，提供连续的“旅途感”。
*   **多种题型**：
    *   **Story Card**：点击翻转查看中文翻译，集成 **Web Speech API** 提供真实英语朗读。
    *   **Quiz Card**：选择题互动，答对/答错反馈，内置解析。
    *   **Matching Card**：左右连线匹配游戏，增加趣味性。
*   **真实评分系统**：
    *   根据一次答对率计算得分。
    *   结算页动态评星（1-3星）及鼓励语。

## 🛠️ 技术栈

*   **框架**: [Next.js 15 (App Router)](https://nextjs.org/)
*   **语言**: TypeScript
*   **样式**: Tailwind CSS v4
*   **动画**: Framer Motion (负责所有转场、翻转、连线动画)
*   **图标**: Lucide React
*   **语音**: Web Speech API (浏览器原生 TTS)

## 📂 目录结构

```
.
├── app/
│   ├── page.tsx               # 首页视频流
│   ├── challenge/
│   │   ├── loading/           # 过渡加载动画
│   │   ├── quiz/              # 核心课程交互页
│   │   └── result/            # 结算评分页
│   └── layout.tsx             # 全局布局
├── components/
│   ├── VideoItem.tsx          # 单个视频组件（含播放逻辑）
│   └── lesson/                # 课程相关组件
│       ├── StorySlide.tsx     # 故事卡片（翻转+朗读）
│       ├── QuizSlide.tsx      # 题目卡片
│       └── MatchingSlide.tsx  # 连线游戏卡片
├── lib/
│   └── data.ts                # Mock 数据中心 (视频+题库)
└── public/
    └── videos/                # 本地视频素材
```

## 🚀 快速开始

1.  **安装依赖**
    ```bash
    npm install
    ```

2.  **启动开发服务器**
    ```bash
    npm run dev
    ```

3.  **访问**
    打开浏览器访问 `http://localhost:3000`。建议使用**手机模式** (Chrome DevTools -> Ctrl+Shift+M) 获得最佳体验。

## 🧪 测试

本项目包含核心组件的单元测试。

```bash
npx vitest run
```

---
Generated by Gemini Agent