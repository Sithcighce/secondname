# 名可名

> 道可道，非常道。名可名。我们希望能真正打开语言到世界的窗，不要你去适应教材，我们要让教材来适应你的世界。

**名可名** 是一款模拟 "抖音插件" 体验的语言学习应用。我们不生产内容，我们只做内容的搬运工和重构者。依托抖音庞大的内容生态，我们将你感兴趣的短视频——无论是美妆测评还是科技开箱——一键转化为为你量身定制的语言学习资料。

## 📖 产品故事

应该每人班里都有个从不学习，只靠刷美剧次次满分的英语大神。语言学习一定是靠兴趣。

**但反观现实，我们收到的教育是什么？** 背单词？刷题？ 还是给 “李华” 写推荐信？

似乎大家忘记了，语言不是死记硬背，语言应该是人类描述世界的本能。

所以，我们做了这款产品。

**想象一下：**
当你在刷抖音时，看到任何一个你感兴趣的内容。不管原始视频是中文还是英文，**你只需要“一键”，就能把它转到我们的 App 中。**

系统会自动根据你的语言水平，把视频变成为你量身定制的、有趣的学习资料。学完之后？你可以毫无负罪感地回去接着刷视频。

我们的愿景很简单：**没有李华，没有枯燥的教材。只有你喜欢的博主，和你最想学的内容。**

我们要做的，就是让语言成为你的 **Second Nature**。让你感兴趣的世界，变得名可名。

## 🧠 核心引擎：去噪、分级、重构

具体怎么做到的？我们构建了 **“去噪、分级、重构”** 的智能体引擎：

1.  **去噪 (De-noise)**: 我们的 NLP 算法会过滤掉视频中的废话和噪音，只提取核心内容，翻译为最地道学习语料。
2.  **分级 (Grade)**: 系统会针对用户实时水平进行难度匹配，确保内容既有挑战性又可理解。
3.  **重构 (Reconstruct)**: 利用大模型技术，生成交互式的关于剧情、情绪和语境的趣味学习内容。

*目前支持英语，未来计划支持日语、希腊语等多语言。*

## ✨ 功能演示 (Demo Features)

虽然我们的愿景是做插件，但为了演示完整的闭环体验，本项目目前包含了一个**模拟的视频流前端**和一个**自动化的后端内容生成管道**。

### 1. 沉浸式视频流 (Feed)
*   **模拟抖音环境**：全屏竖向滑动切换视频 (CSS Scroll Snap)，还原用户日常刷视频的体验。
*   **智能播放控制**：滑入自动播放，滑出自动暂停；优先尝试有声播放。
*   **无限刷**：模拟无限数据流，支持循环滑动。

### 2. 交互式课程 (Interactive Lesson)
*   **一键转化**：点击视频右上角的 "Daily Quiz" (模拟插件入口)，平滑进入课程。
*   **时间轴模式 (Timeline UI)**：
    *   摒弃传统翻页，采用**垂直流式生长**布局。
    *   新内容出现时自动平滑滚动对齐，提供连续的“旅途感”。
*   **多维互动**：
    *   **Story Card**：基于视频剧情的双语故事，集成 **Web Speech API** 提供真实英语朗读。
    *   **Quiz Card**：情境选择题，答对/答错反馈，内置解析。
    *   **Matching Card**：左右连线匹配游戏，增加趣味性。

### 3. 后端数据 Pipeline (Content Engine)
为了实现“重构”，我们开发了 `backend_pipeline/` 工具箱，用于从原始 MP4 视频自动生成结构化的双语课程 JSON 数据。

*   **视频处理**: 自动提取 MP4 视频中的音轨。
*   **语音识别 (ASR)**: 调用 **SiliconFlow API** (SenseVoice 模型) 将音频转录为文字稿。
*   **智能生成 (LLM)**: 调用 **SiliconFlow API** (Qwen 2.5 72B) 分析文字稿，并严格按照前端所需的 JSON Schema 生成：
    *   逐句的双语故事 (Story)
    *   情境选择题 (Quiz)
    *   词汇连线题 (Matching)

## 🛠️ 技术栈

*   **前端**: [Next.js 15 (App Router)](https://nextjs.org/), TypeScript, Tailwind CSS v4, Framer Motion
*   **后端脚本**: Python, SiliconFlow API (DeepSeek/Qwen/SenseVoice)
*   **其他**: Lucide React (图标), Web Speech API (语音合成)

## 📂 目录结构

```
.
├── app/
│   ├── page.tsx               # 首页视频流 (模拟抖音 Feed)
│   ├── challenge/
│   │   ├── loading/           # 过渡加载动画
│   │   ├── quiz/              # 核心课程交互页
│   │   └── result/            # 结算评分页
│   └── layout.tsx             # 全局布局
├── components/
│   ├── VideoItem.tsx          # 单个视频组件（含播放逻辑）
│   └── lesson/                # 课程相关组件
│       ├── StorySlide.tsx     # 故事卡片（翻转+朗读）
│       ├── QuizSlide.tsx      # 题目卡片
│       └── MatchingSlide.tsx  # 连线游戏卡片
├── lib/
│   └── data.ts                # 数据中心 (Video + Lesson Mock Data)
├── public/                    # 静态资源 (视频/图片)
└── backend_pipeline/          # Python 自动化内容生成工具
    ├── generate_lesson.py     # 主生成脚本
    └── MP4_to_MP3.py          # 格式转换工具
```

## 🚀 快速开始

1.  **安装依赖**
    ```bash
    npm install
    ```

2.  **启动开发服务器**
    ```bash
    npm run dev
    ```

3.  **访问**
    打开浏览器访问 `http://localhost:3000`。建议使用**手机模式** (Chrome DevTools -> Ctrl+Shift+M) 获得最佳体验。

## 🧪 测试

本项目包含核心组件的单元测试。

```bash
npx vitest run
```

---
